/**
 * 数据分析师场景 - 组件注册
 *
 * 场景特有 UI 组件的注册入口。
 * 后续可扩展：DataDashboard、ChartGalleryView、AnalysisHistoryView 等。
 */

import type { ScenarioComponentRegistry } from '../../types'

export const dataAnalystComponents: ScenarioComponentRegistry = {}
