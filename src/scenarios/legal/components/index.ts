import type { ScenarioComponentRegistry } from '@shared/protocols/scenario-arch'
import { ContractReviewPanel } from './ContractReviewPanel'
import { ComplianceDashboard } from './ComplianceDashboard'

export const legalComponents: ScenarioComponentRegistry = {
  ContractReviewPanel,
  ComplianceDashboard,
}
