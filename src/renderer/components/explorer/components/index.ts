/**
 * Explorer 内联创建组件入口
 *
 * 此模块作为资源管理器（Explorer）中内联创建类组件的统一出口，
 * 采用具名导出以便 Tree-Shaking 优化。
 *
 * @packageDocumentation
 */

export { InlineCreateInput as ExplorerInlineCreateInput } from './InlineCreateInput'
