# AweeClaw 客户端架构整改计划

> 生成时间：2026-06-05
> 状态标记：[ ] 待处理 | [~] 进行中 | [x] 已完成

---

## 一、整改总览

| 优先级 | 类别 | 问题数 | 已完成 | 完成率 |
|--------|------|--------|--------|--------|
| P0 | 安全/稳定性 | 4 | 4 | 100% |
| P1 | 架构/性能 | 5 | 5 | 100% |
| P2 | 维护性 | 3 | 3 | 100% |

---

## 二、P0 - 立即整改（安全/稳定性）

### P0-1: 收紧 CSP 安全策略
- **状态**: [x] 已完成
- **文件**: `src/main/appBootstrap.ts`
- **问题**: CSP 策略过于宽松，`script-src 'unsafe-inline' 'unsafe-eval'`、`connect-src 'self' https:`、`frame-src https:` 存在 XSS 和数据外泄风险
- **方案**:
  1. 移除 `unsafe-eval`（Monaco 硬性依赖，保留但添加注释）
  2. `frame-src` 限制为本地开发服务器
  3. 新增 `object-src 'none'`、`base-uri 'self'`、`form-action 'self'` 防御策略
- **影响范围**: 主进程 CSP 配置

### P0-2: API Key 加密存储
- **状态**: [x] 已完成
- **文件**: `src/main/modules/settings-db/SettingsDb.ts`、`src/main/guard/safeStorageUtil.ts`（新建）
- **问题**: API Key 明文存储在 SQLite 和 localStorage 中，renderer 进程可直接读取
- **方案**:
  1. 新建 `safeStorageUtil.ts`，封装 Electron safeStorage 加密/解密
  2. SettingsDb 写入时加密 API Key 和渠道凭证
  3. SettingsDb 读取时解密返回
  4. renderer 通过 IPC 获取解密后的值
- **影响范围**: 设置持久化层、IPC 通道

### P0-3: 清除 console.log 残留
- **状态**: [x] 已完成
- **文件**: 20+个业务文件
- **问题**: 生产环境日志泄露，应统一走 LogEngine
- **方案**:
  1. 全局替换 `console.log/warn/error` 为 `logger.xxx`（按模块分类）
  2. 保留进程级兜底（appBootstrap uncaught exception）和 Logger 自身 fallback
  3. 保留代码模板/片段中的 console（snippetAdapter、scenario-system/cli）
- **影响范围**: 全局业务代码

### P0-4: 修复空 catch 块
- **状态**: [x] 已完成
- **文件**: 20+个文件，包括 themeDefinition、toolExecutors、IntelligenceCore、languageServerManager、scenarioInstall、WeixinChannelPlugin、authSlice、backendApi 等
- **问题**: 多处 `catch {}` 静默吞没错误，影响问题排查
- **方案**: 补充 `logger.xxx.warn/error/debug` 记录异常信息，按模块分类
- **影响范围**: 全局错误处理

---

## 三、P1 - 短期改进（架构/性能）

### P1-1: 拆分 AweeApp.tsx 布局逻辑
- **状态**: [x] 已完成
- **文件**: `src/renderer/AweeApp.tsx` → 新建 `SidebarSection.tsx`、`ChatSection.tsx`、`MainContentArea.tsx`、`GlobalOverlays.tsx`
- **问题**: 636行，两种 `chatPosition` 布局模式（primary/secondary）有大量重复的 JSX 结构
- **方案**: 提取 `SidebarSection`（侧边栏+拖拽）、`ChatSection`（聊天面板+拖拽）、`MainContentArea`（主内容路由，含 PrimaryMainContent/SecondaryMainContent）、`GlobalOverlays`（全局弹窗），AweeApp 从 636 行缩减至 230 行
- **影响范围**: 根组件、布局系统

### P1-2: shell.openExternal URL 协议白名单校验
- **状态**: [x] 已完成
- **文件**: `src/main/guard/safeExternalUrl.ts`（新建）、`appBootstrap.ts`、`updateManager.ts`、`ToolProtocolManager.ts`、`filePermissionGuard.ts`
- **问题**: URL 清理仅去除首尾特殊字符，未做协议白名单限制，存在协议注入风险
- **方案**: 新建 `safeOpenExternal` 统一工具，白名单限制 http/https/mailto/devtools 协议，所有 shell.openExternal 调用统一走此工具
- **影响范围**: 主进程所有外部链接打开

### P1-3: 消除 PreferencesDialog 中 as any 类型
- **状态**: [x] 已完成
- **文件**: `src/renderer/components/settings/PreferencesDialog.tsx`
- **问题**: 使用 `as any` 绕过类型检查
- **方案**: 替换为 `as Partial<PersistedLLMConfig>`，引入正确类型
- **影响范围**: 设置对话框

### P1-4: 重型依赖懒加载
- **状态**: [x] 已完成
- **文件**: `AgentWorkspace.tsx`、`BillingCenterPage.tsx`、`DocumentPreview.tsx`
- **问题**: `three`/`@react-three/fiber`/`echarts`/`xlsx` 等重型依赖全部同步打包，影响首屏加载
- **方案**: TeamOffice（three.js）改为 `React.lazy`，UsagePanel（echarts）改为 `React.lazy`，xlsx 改为 `await import('xlsx')` 动态加载
- **影响范围**: 3D 场景、图表、表格预览组件

### P1-5: localStorage 统一抽象层
- **状态**: [x] 已完成（核心文件）
- **文件**: 新建 `src/shared/toolkit/StorageService.ts`，已替换 `authSlice.ts`、`themeSlice.ts`、`backendApi.ts`、`themeDefinition.ts`
- **问题**: localStorage 散用，无统一抽象，数据迁移和清理困难
- **方案**: 创建 `StorageService` 统一封装，支持类型安全、命名空间隔离、TTL 过期、版本迁移；核心文件已替换，其余文件后续迭代逐步替换
- **影响范围**: 全局存储访问

---

## 四、P2 - 中期优化（维护性）

### P2-1: 拆分 IntelligenceCore 单体类
- **状态**: [x] 已完成
- **文件**: `src/renderer/intelligence/engine/IntelligenceCore.ts` → 新建 `MultiAgentExecution.ts`
- **问题**: 1628行，send/executeMultiAgent/continueMultiAgent 全在一个类中
- **方案**: 将 `executeMultiAgent` 和 `continueMultiAgent` 提取到 `MultiAgentExecution.ts`，共享 `callLLM` 和 `executeAgent` 工厂函数，IntelligenceCore 从 1628 行缩减至 620 行
- **影响范围**: Agent 核心引擎

### P2-2: PreferencesDialog 状态管理重构
- **状态**: [x] 已完成
- **文件**: `src/renderer/components/settings/PreferencesDialog.tsx` → 新建 `useSettingsLocalState.ts`
- **问题**: 738行，19 个 useState，状态管理复杂
- **方案**: 创建 `useSettingsLocalState` Hook，将 19 个 useState 统一为 useReducer，集中管理 isDirty 和 handleSave 逻辑；PreferencesDialog 从 738 行缩减至 377 行
- **影响范围**: 设置对话框

### P2-3: 补充核心路径单元测试
- **状态**: [x] 已完成
- **文件**: 新建 `tests/unit/shared/StorageService.test.ts`、`tests/unit/main/safeExternalUrl.test.ts`、`tests/unit/renderer/settingsReducer.test.ts`
- **问题**: 核心业务无测试覆盖
- **方案**: 为 StorageService（19个用例）、safeExternalUrl（18个用例）、settingsReducer（12个用例）编写单元测试，覆盖正常/边界/异常场景
- **影响范围**: 测试体系

---

## 五、变更记录

| 日期 | 整改项 | 变更内容 |
|------|--------|----------|
| 2026-06-05 | 初始创建 | 生成整改计划文档 |
| 2026-06-05 | P0 全部完成 | CSP策略收紧、safeStorage加密、console清理、空catch修复 |
| 2026-06-05 | P1-2~P1-5 完成 | URL白名单、类型修复、懒加载、StorageService统一封装 |
| 2026-06-05 | P2-1 完成 | IntelligenceCore拆分，1628行→620行，提取MultiAgentExecution模块 |
| 2026-06-05 | P1-1 完成 | AweeApp布局拆分，636行→230行，提取4个子组件 |
| 2026-06-05 | P2-2 完成 | PreferencesDialog状态重构，738行→377行，19个useState→useReducer |
| 2026-06-05 | P2-3 完成 | 补充核心路径单元测试，49个用例全部通过 |
